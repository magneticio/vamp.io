box: wercker/default
build:
  steps:
    - script:
        name: config status
        code: >
          if [ -e "config" ]; then
          echo "config exists";
          else
          echo "config does not exist";
          fi;
    - arjen/hugo-build:
        version: 0.13
        flags: --buildDrafts=false
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: vamp.io
        basedir: dist



    - arjen/hugo-build:
        version: 0.13
        flags: --buildDrafts=false
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: vamp.io
        basedir: dist
